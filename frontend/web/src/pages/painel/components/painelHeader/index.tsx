import React from 'react';

import {
    HeaderContainer,
    HeaderTitle
} from './styles';

const PainelHeader = () => {

    return(
        <HeaderContainer>
            <HeaderTitle>Records</HeaderTitle>
        </HeaderContainer>
    );
};

export default PainelHeader;